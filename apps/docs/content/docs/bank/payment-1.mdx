---
title: Badge
description: A component to show different Badges in our mobile.
---

import { ComponentPreview } from "@/src/components/ui/component-preview";
import { SourceCode } from "@/src/components/ui/source-code";
import { APITable } from "@/src/components/docs/api-table";
import { PackageManagers } from "@/src/components/docs/package-manager";
import { blockPreview } from "@/src/components/ui/block-preview";
import { DocTab, DocTabs } from "@/src/components/docs/tab";

import {
  CardOne,
  PaymentFour,
  PaymentThree,
  PaymentTwo,
  PricingOne,
  PricingTwo,
  PaymentOne,
} from "@sol-ui/bank-kit/blocks";

<DocTabs items={["Preview", "Code"]} >
  <DocTab value="Preview" >
    <ComponentPreview>
          <PaymentOne />
    </ComponentPreview>
  </DocTab>
  <DocTab value="Code">
  <SourceCode content={
    `
import React, { useState } from "react"
import { View, Text, Pressable} from "react-native"
import AntDesign from 'react-native-vector-icons/AntDesign';

export function InfoBadge({ info = "Buy Coffee" }: { info: string }) {
    return (
        <View className=" w-64 h-60 bg-black/5 rounded-2xl p-1">
            <View className="w-full h-full bg-white border border-neutral-300 rounded-[12px] flex items-center justify-center ">
                <Text>
                    <AntDesign name="warning" size={20} /> 
                </Text>
                <Text className="font-semibold">
                    To your attention!
                </Text>
                <Text className="text-neutral-500 text-xl my-1">
                    {
                        info
                    }
                </Text>
                <View className="w-48 bg-black/5 py-1 px-1  my-5 rounded-md mb-0 ">
                    <Pressable className="relative bg-green-100/80 px-5 py-2 w-fit rounded-md flex flex-row justify-end  ">
                        <Text className="font-semibold">
                            Accept
                        </Text>
                    </Pressable>
                    <Pressable className="absolute top-1 h-full w-20 shadow-sm border border-black/5 bg-white px-3 rounded-md active:translate-x-[96px]">
                        <View className="absolute inset-0 border-r border-black/50  right-10 top-1 bottom-1">

                        </View>
                        <View className="absolute inset-0 border-r border-black/50  right-8 top-1 bottom-1">

                        </View>
                        <View className="absolute inset-0 border-r border-black/50  right-6 top-1 bottom-1">

                        </View>
                        <View className="absolute inset-0 border-r border-black/50  right-4 top-1 bottom-1">

                        </View>
                    </Pressable>

                </View>
            </View>
        </View>
    )

}
`
}/>

  </DocTab>
</DocTabs>

## Installation

<DocTabs items={[ 'Command' , 'Agents']}>
<DocTab value="Command">

<PackageManagers
  commands={{
    cli: "npx solui@latest add badge",
  }}
/>
  </DocTab>
  <DocTab value="Agents">
    Use any MCP-compatible coding agent to install this component. Ask your agent:

    > Install the `payment-1` Sol UI component.

  </DocTab>
</DocTabs>

## Usage

```tsx
import { InfoBadge } from "@/components/ui/infoBadge";

export default function InfoBadgeDemo() {
  return <InfoBadge info="Buy Coffee" />;
}
```

## Default badge

<blockPreview>
  <img src="/ui-demo-preview/badge-1.jpg" width={200} height={200} />
</blockPreview>

```tsx
import { DefaultBadge } from "@/components/badges/badge1";

export default function DefaultBadgeDemo() {
  return <DefaultBadge BadgeValue="Default" />;
}
```

<PackageManagers
  commands={{
    cli: "npx solui@latest add badge-1",
  }}
/>

## Spinning badge

<blockPreview>
  <img src="/ui-demo-preview/badge-2.jpg" width={200} height={200} />
</blockPreview>

```tsx
import { SpinningBadge } from "@/components/badges/badge2";

export default function SpinningBadgeDemo() {
  return <SpinningBadge BadgeValue="Loading" />;
}
```

<PackageManagers
  commands={{
    cli: "npx solui@latest add badge-2",
  }}
/>

## Count badge

<blockPreview>
  <img src="/ui-demo-preview/badge-3.jpg" width={200} height={200} />
</blockPreview>

```tsx
import { CountBadge } from "@/components/badges/badge3";

export default function CountBadgeDemo() {
  return <CountBadge Count={2} imgUrl="default.jpg" />;
}
```

<PackageManagers
  commands={{
    cli: "npx solui@latest add badge-3",
  }}
/>

## Warning badge

<blockPreview>
  <img src="/ui-demo-preview/badge-4.jpg" width={200} height={200} />
</blockPreview>

```tsx
import { WarningBadge } from "@/components/ui/WarningBadge";

export default function WarningBadgeDemo() {
  return <WarningBadge title="Download again" />;
}
```

<PackageManagers
  commands={{
    cli: "npx solui@latest add badge-4",
  }}
/>

## Error badge

<blockPreview>
  <img src="/ui-demo-preview/badge-5.jpg" width={200} height={200} />
</blockPreview>

```tsx
import { ErrorBadge } from "@/components/ui/ErrorBadge";

export default function ErrorBadgeDemo() {
  return <ErrorBadge title="User not found" />;
}
```

<PackageManagers
  commands={{
    cli: "npx solui@latest add badge-5",
  }}
/>
