{
  "name": "sol-ui",
  "private": true,
  "workspaces": [
    "apps/*",
    "apps/packages/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "start": "turbo run start",
    "test": "npm run test --workspace=@sol-ui/component-installer && npm run test --workspace=@sol-ui/mcp"
  },
  "devDependencies": {
    "turbo": "^2"
  },
  "packageManager": "npm@10.8.2"
}
